@model GuessSecretNumber.Models.SecretNumber

@using (Html.BeginForm())
{
    // skydd, rätt avsändare
    @Html.AntiForgeryToken()
    // Visar felmeddelande
    @Html.ValidationSummary(true)

    @Html.ValidationMessageFor(model => model.NewNumber)
    <br />
    <br />
    @Html.LabelFor(model => model.NewNumber)
    @Html.EditorFor(model => model.NewNumber)

    <input type="submit" value="Skicka gissning" />
}
